--- src/services/execution/main.py
+++ src/services/execution/main.py
@@ -71,11 +71,2 @@
     input_data = {name: np.atleast_1d(val) for name, val in inputs.items()}
     out_data = prob.execute(input_data)
-    results = {}
-    for obj in objectives:
-        val = out_data.get(obj)
-        if val is None:
-            results[obj] = 0.0
-        else:
-            results[obj] = val
-    return results
+    return {obj: 0.0 if (val := out_data.get(obj)) is None else val for obj in objectives}
